<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bot Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-900 text-white min-h-screen">
    <nav class="bg-gray-800 border-b border-gray-700">
        <div class="container mx-auto px-6 py-4 flex items-center justify-between">
            <a href="/" class="text-xl font-bold flex items-center gap-2">
                <i class="fas fa-robot text-blue-500"></i>
                <span>Bot Dashboard</span>
            </a>
            <% if (user) { %>
                <div class="flex items-center gap-4">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" class="w-8 h-8 rounded-full">
                    <span><%= user.username %></span>
                    <a href="/logout" class="text-gray-400 hover:text-white">Logout</a>
                </div>
            <% } else { %>
                <a href="/login" class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded-lg">
                    <i class="fab fa-discord mr-2"></i>Login with Discord
                </a>
            <% } %>
        </div>
    </nav>

    <main class="container mx-auto px-6 py-20">
        <div class="text-center max-w-3xl mx-auto">
            <div class="w-24 h-24 mx-auto mb-8 bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl flex items-center justify-center">
                <i class="fas fa-sliders-h text-4xl"></i>
            </div>
            <h1 class="text-4xl font-bold mb-4">Bot Dashboard</h1>
            <p class="text-gray-400 text-lg mb-8">Kontrol dan konfigurasi bot Discord kamu dengan mudah melalui web dashboard yang modern dan intuitif.</p>
            
            <% if (!user) { %>
                <a href="/login" class="inline-flex items-center gap-2 bg-blue-600 hover:bg-blue-700 px-8 py-3 rounded-xl text-lg">
                    <i class="fab fa-discord"></i>
                    <span>Login dengan Discord</span>
                </a>
            <% } else { %>
                <a href="/dashboard" class="inline-flex items-center gap-2 bg-blue-600 hover:bg-blue-700 px-8 py-3 rounded-xl text-lg">
                    <i class="fas fa-cog"></i>
                    <span>Buka Dashboard</span>
                </a>
            <% } %>
        </div>

        <div class="grid md:grid-cols-3 gap-6 mt-20 max-w-4xl mx-auto">
            <div class="bg-gray-800 border border-gray-700 rounded-xl p-6 text-center">
                <div class="w-14 h-14 mx-auto mb-4 bg-green-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-cogs text-2xl text-green-500"></i>
                </div>
                <h3 class="font-semibold mb-2">Easy Config</h3>
                <p class="text-gray-400 text-sm">Konfigurasi bot tanpa coding</p>
            </div>
            <div class="bg-gray-800 border border-gray-700 rounded-xl p-6 text-center">
                <div class="w-14 h-14 mx-auto mb-4 bg-blue-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-shield-alt text-2xl text-blue-500"></i>
                </div>
                <h3 class="font-semibold mb-2">Secure</h3>
                <p class="text-gray-400 text-sm">Login aman dengan Discord OAuth</p>
            </div>
            <div class="bg-gray-800 border border-gray-700 rounded-xl p-6 text-center">
                <div class="w-14 h-14 mx-auto mb-4 bg-purple-500/20 rounded-xl flex items-center justify-center">
                    <i class="fas fa-sync text-2xl text-purple-500"></i>
                </div>
                <h3 class="font-semibold mb-2">Real-time</h3>
                <p class="text-gray-400 text-sm">Perubahan langsung aktif</p>
            </div>
        </div>
    </main>
</body>
</html>
